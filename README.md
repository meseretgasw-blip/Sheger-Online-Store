# Sheger Online Store

Animated static website concept for Sheger Online Store.

- 21st.dev-inspired interaction language: Bento Grid + animated product cards
- Theme: Electric cobalt marketplace
- No external runtime dependencies
- Cloudflare Workers Static Assets ready
- Real business imagery can be added later under `assets/` without changing the layout system

## Run

Deploy with Wrangler using `npx wrangler deploy`.
